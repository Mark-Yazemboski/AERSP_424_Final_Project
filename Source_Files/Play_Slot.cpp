#include "Gambler.h"
#include "SlotMachine.h"
#include "Card.h"
#include <iostream>
#include <vector>
#include <string>

//This is the file that would play a slot machine on the cmd window. there was no visual.
//After doing the snake visual there was a better chance of me not selling my soul to lockheed
//Than to make a visual for this.

/*
// Main function
int main() {
    // Create cards with symbols and image files
    std::vector<Card> cards = {
        Card("🍒", "cherry.png",2.5),
        Card("🍋", "lemon.png",1.5),
        Card("🍋", "lemon.png",1.5),
        Card("🍋", "lemon.png",1.5),
        Card("🍊", "orange.png",1.5),
        Card("💎", "diamond.png",10),
        Card("⭐", "star.png",4),
    };

    // Create a slot machine with 3 reels and the card set
    SlotMachine machine(3, cards);

    // Create a gambler
    Gambler gambler("Mark", 100);

    std::cout << "Welcome to the Slot Machine, " << gambler.getName() << "!" << std::endl;

    // Play the slot machine
    gambler.playSlotMachine(machine, 10);
    gambler.playSlotMachine(machine, 20);
    gambler.playSlotMachine(machine, 20);
    gambler.playSlotMachine(machine, 20);
    gambler.playSlotMachine(machine, 20);

    return 0;
}*/